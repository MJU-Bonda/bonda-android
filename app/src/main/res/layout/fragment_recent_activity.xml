<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="19dp"
        android:paddingHorizontal="19dp"
        android:visibility="gone"
        app:spanCount="2" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/empty_book_list_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="아직 조회한 도서가 없습니다.\n본다에서 다양한 도서를 찾아 보세요."
        android:textAlignment="center"
        android:textAppearance="@style/Body5"
        android:textColor="@color/text_default_secondary"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/empty_article_list_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="아직 조회한 아티클이 없습니다.\n본다에서 다양한 아티클을 찾아 보세요."
        android:textAlignment="center"
        android:textAppearance="@style/Body5"
        android:textColor="@color/text_default_secondary"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/progress_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <include
        android:id="@+id/error_network"
        layout="@layout/layout_error_network"
        android:visibility="gone" />

    <include
        android:id="@+id/error_common"
        layout="@layout/layout_error_common"
        android:visibility="gone" />

</androidx.constraintlayout.widget.ConstraintLayout>